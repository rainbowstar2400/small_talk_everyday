# Neccho向け案内

`\demo\`の中で作業してほしい！

ここの中にHTMLファイルとCSSファイルを入れる感じ

メモファイル（`.txt`）とかを入れても大丈夫


これはコミットのデモ
無視してね

import { useState } from "react";
import"./App.css";
function Login({onLogin}) {
  return (
    <div className="login-page">
      <div className="login-box">
        <h1>ユーザー名とパスワードを入力してログイン</h1>

        <input type="text" placeholder="ユーザー名" />
        <input type="password" placeholder="パスワード" />
        </div>
      <button className="login-button" onClick={onLogin}>
        ログイン
      </button>
      </div>
  );
}
function Home({onLogout}) {
  return (
    <div className="home-page">
      <div className="home-card">
        <h1>Welcome back! Ready to speak?</h1>
        
        <div className="level-row">
          <p>今週のレベル</p>
          <div className="level-box">レベル３</div>
       </div>
      </div>

      <div className="progress-area">
        <div className="progress-bar">
          <div className="progress-done">60%</div>
        </div>
      <div className="pin"></div>
    </div>

    <div className="week-grid">
      <div className="day-card done-card">
        <h2>Monday</h2>
      <div className="done-stamp">DONE</div>
      </div>

      <div className="day-card done-card">
        <h2>Tuesday</h2>
        <div className="done-stamp">DONE</div>
     </div>

    <div className="day-card today-card">
      <h2>Wednesday</h2>
       <button className="start-button">TAP<br />TO<br /> START</button>
       </div>

       <div className="day-card locked-card">
        <h2>Thursday</h2>
      </div>

      <div className="day-card locked-card">
        <h2>Friday</h2>
      </div>
    </div>
  <button className="logout-button" onClick={onLogout}>
        ログアウト
  </button>
  </div>
  );
}


function App(){
const [isLoggedIn, setIsLoggedIn] = useState(false);

return(
  <>
  {isLoggedIn?(
    <Home onLogout={()=>setIsLoggedIn(false)} />
  ) : (
    <Login onLogin={()=> setIsLoggedIn(true)} />
  )}
  </>
);
}
export default App;
